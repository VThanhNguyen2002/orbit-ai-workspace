{
  "name": "mobile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"Expo app is not initialized yet\"",
    "lint": "eslint . --max-warnings=0 --no-error-on-unmatched-pattern",
    "typecheck": "echo \"Mobile typecheck placeholder\"",
    "test": "echo \"No tests yet for mobile\"",
    "build": "echo \"Mobile build placeholder\""
  },
  "dependencies": {
    "@synapse/api-client": "workspace:*",
    "@synapse/config": "workspace:*",
    "@synapse/shared": "workspace:*",
    "@synapse/ui": "workspace:*"
  }
}
